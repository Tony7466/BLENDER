# SPDX-FileCopyrightText: 2023 Blender Authors
#
# SPDX-License-Identifier: GPL-2.0-or-later

set(INC
  .
  importer
  ../common
  ../../blenkernel
  ../../bmesh
  ../../editors/include
  ../../makesrna
  ../../windowmanager
)

set(INC_SYS
  ../../../../extern/fast_float
)

set(SRC
  IO_csv.cc
  importer/csv_data.cc
  importer/csv_import.cc
  importer/csv_reader.cc
  importer/csv_string_utils.cc

  IO_csv.hh
  importer/csv_data.hh
  importer/csv_import.hh
  importer/csv_reader.hh
  importer/csv_string_utils.hh
)

set(LIB
  bf_blenkernel
  PRIVATE bf::blenlib
  PRIVATE bf::depsgraph
  PRIVATE bf::dna
  PRIVATE bf::intern::guardedalloc
  bf_io_common
  PRIVATE bf::extern::fmtlib
)

blender_add_lib(bf_io_csv "${SRC}" "${INC}" "${INC_SYS}" "${LIB}")

if(WITH_GTESTS)
  set(TEST_SRC

  )

  set(TEST_INC
    ${INC}

    ../../blenloader
    ../../../../tests/gtests
  )

  set(TEST_LIB
    ${LIB}

    bf_blenloader_test_util
    bf_io_csv
  )

  blender_add_test_suite_lib(io_csv "${TEST_SRC}" "${TEST_INC}" "${INC_SYS}" "${TEST_LIB}")
endif()
